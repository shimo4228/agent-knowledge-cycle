# Scaffold Dissolution — AKC スキルの自己消滅性

## Concept

AKC のスキル群は **足場 (scaffold)** である。サイクルを回すうちにユーザーと AI の両方がサイクルを理解し、対話の中で自然に必要なことを行うようになる。やがてスキルを明示的に呼ぶ必要がなくなる。

```
Phase 1: スキルが行動を教える
  /skill-stocktake → 棚卸しの手順を知る
  /skill-comply → 遵守率を計測する方法を知る

Phase 2: サイクルが身体化される
  スキルを呼ばなくても、対話の中で棚卸し・遵守チェックが自然に起きる
  ルール (testing.md 等) とメモリ (feedback 等) にサイクルの原則が蓄積される

Phase 3: スキルは不要になる
  ユーザー: サイクルの思考パターンが定着
  AI: ルールとメモリからサイクルの原則を再構成できる
  スキルという形式化が冗長になる
```

## Evidence

### 2026-03-29 — スキルなしでサイクルが回った

2026-03-29 のセッションで、AKC の全フェーズが対話的に実行された。どのスキルも明示的に呼ばれなかった:

| AKC スキル | フェーズ | 対話で行われたこと |
|-----------|---------|-----------------|
| search-first | Research | ECC v1.9.0 の差分を調べ、Python 関連を評価 |
| skill-stocktake | Curate | 33 スキルを 1 個ずつ対話で判定 → 16 に削減 |
| skill-comply | Measure | 「eval-harness は testing.md と重複」と発見 |
| rules-distill | Promote | 整理の中で feedback_python_review を memory に保存 |
| learn-eval | Extract | 2 つの記事ドラフトとして知見を外部化 |
| context-sync | Maintain | /context-sync を最後に実行し整合性を確認 |

### 2026-06-10 — プラットフォーム吸収による溶解

3 ヶ月後、当初の概念が想定していなかった第二の溶解経路が観測された。個人ハーネス (Claude Code、ECC エコシステム上に構築) の棚卸しで、ホストプラットフォームが手組みの検証ループのネイティブ版を出荷していたことが判明した: built-in の `verify` / `code-review` / `security-review` skill と native な plan mode である。わずか 3 ヶ月前に工夫を重ねて組んだローカル版は、名前衝突する重複物になっていた。

結果: 6 skill と 1 rule ファイルを削除 (さらに stale な節を trim)、正味 −1,429 / +124 行。明示的な退役基準 (built-in 重複 / モデル知識による自明化 / バージョン依存記述) とともにハーネス側の ADR に記録された。棚卸し自体は素朴な会話のリクエストから始まり、`/skill-stocktake` を呼ぶことなく Curate フェーズが最後まで走った — 2026-03-29 の観測の再現である。

棚卸しを生き残ったものは明快な法則に従う (後述「What Survives」参照): 453 行の OWASP チェックリスト (モデル知識との差分ゼロ) は退役し、45 行の個人用セッション起動 skill (差分 100%) は残った。

### 2026-07-09 — telemetry で初めて実証された退役 (rules 層への吸収)

第三の観測が、溶解と計測のループを閉じた。運用者のハーネスは決定論的な usage telemetry 層 (Measure) を持ち、その出力は Curate の silence check に流れる。1 ヶ月分のログは、3 つの design-pattern skill の 1 つである signal-first-research の organic な発火がゼロであることを示した: 明示的な起動はなく、ファイル read はすべて監査または cross-reference セッション由来で、実作業での使用は一度もなかった。原則そのものは毎日発火していた — ただし常時ロードの rules 層 (Research フェーズの rule) と search-first フェーズ skill を通してである。skill の operational な中身は完全に吸収されていた: 内面化経路の完了が、初めて計器の上で可視化された。

同日、ローカルの install は退役し、参照は rules 層の正本に付け替えられた。公開リポジトリは citable な design-pattern artifact として無変更で存続する — 溶解が取り除くのは install された足場であって、記録された judgment ではない。この観測が先の 2 つと異なる点は 2 つ: 退役の判断が語りの印象ではなく計器 (silence check) によって下されたこと — 後述の「observable, not just narratable」基準が実地で満たされた — そして Implication 1 の直接の確認になっていること — 使用ゼロが、まさにそれが意味する成功シグナルとして読まれた。

### 2026-07-25 — モデル能力の世代交代による溶解 (足場が有害に転じる)

第四の観測は、この現象の符号を変えた。先の 3 つの経路はいずれも、足場が*冗長*になって終わる — 何も足さないのだから、外すのはタダである。この経路は、足場が*保持すること自体が高くつく*状態で終わる。

トリガーは外因性で、かつ公表されたものだった。Anthropic は、Claude 5 世代向けに Claude Code 自身のシステムプロンプトを 80% 以上削除してもコーディング eval に計測可能な劣化がなかったと報告し、そのメカニズムを名指しした — 弱いモデルの最悪ケースを防ぐために書かれたガードレールが、強いモデルには*矛盾する指示*として届き、その解決に推論を費やさせる (「leave documentation as appropriate」と「DO NOT add comments」の衝突)。制約の前提は失効していたが、そのコストは失効していなかった。

同じ監査を運用者のハーネスに当てると、同じ形が出た。常時ロードの rules 層は 20 ファイル 43,971 文字あり、次のものを抱えていた: 絶対命令として書かれた over-constraint (「ALWAYS create new objects, NEVER mutate」— in-place の数値処理・builder・accumulator では単に誤り)、コードを一行も書かないセッションを含む全セッションに front-load された タスク種別 × 9 ステップの判断マトリクス、同一ファイル内で互いに正本を自称する 2 つの節、そして言語に関係なく常駐する言語固有 rule 6 ファイル — Python を一切含まないセッションでの注入を実測で確認した。rules 層は 19,240 文字 (−56%、20 → 14 ファイル) に落ちたが、行き先のあるものは何も捨てていない: 判断マトリクスは実装着手時にロードされる skill へ移り、言語 rule は同じドメインを既に持っていた skill に畳み込まれ、絶対命令はベンダー自身の置換文言に合わせて緩和された — *周囲のコードと同じように読めるコードを書け*。

この観測を特徴づける点が 2 つある。証拠クラスが **ベンダー開示** だったこと — ローカルの ablation より安価で広範だが、証明する対象が違う。モデルの能力について語っており、この運用者の内面化については語らない。したがって (プラットフォーム吸収と同じく、Implication 5) 制約を一度に全員に対して退役させるが、誰の学習についても何も主張しない。そして退役が **自己処方** だったこと — rules 層は既に Scaffold Dissolution 条項を抱えており、その downward vector は「substrate が扱うようになった領域を覆う手書き rule は、drift した静的コピーとして新しい既定を上書きし劣化させうる」と述べていた。この条項はプラットフォーム吸収のために書かれたものだが、モデル能力の世代交代は substrate の*機能*の代わりに*判断*を置いた同じ論証であり、条項はそれを名指しするよう改訂された。

第三の特徴は集計でのみ計測できる。削減の後、そのハーネスで最大の常駐コストは rules 層ではなく skill listing になった — 60 個の skill description が計 23,473 文字で、listing の予算はコンテキストウィンドウの約 1% である。溶解は制約を取り除くのではなく、移動させる。

## Why This Happens

### ユーザー側の学習

AKC のサイクルを繰り返すうちに、「棚卸しすべきタイミング」「蒸留すべきパターン」「計測すべき指標」を暗黙的に理解する。スキルの手順書を参照しなくても、対話の中で適切な問いを発せられるようになる。

### AI 側の蓄積

サイクルを回すたびに:
- ルール (`rules/`) にサイクルの原則が蒸留される
- メモリ (`memory/`) にフィードバックと判断履歴が蓄積される
- ADR (`docs/adr/`) に設計判断が記録される

これらがコンテキストとしてロードされるため、AI はスキルファイルを読まなくてもサイクルの原則に沿った行動を取れる。

### プラットフォーム側の吸収

ホストプラットフォーム自身が足場を吸収する: ツールやモデルがその能力をネイティブに搭載し、誰かが内面化したかどうかに関係なくローカルの artifact を冗長化する。前の 2 つのメカニズムと違い、これは外因性である — 練習ではなくプラットフォームのリリースサイクルが駆動する。2026-06-10 の観測では、手組みのループから built-in 機能までのラグはおよそ 3 ヶ月だった。

### モデル側の能力の世代交代

足場の機能が重複するのではなく、足場の*前提*が失効する。弱いモデルを最悪ケースから遠ざけるために書かれた制約 — 一律の禁止、網羅的な手順、反復による強調、列挙された使用例 — は、新しいプラットフォームに重複物を持たない。変わったのは、それが防いでいた振る舞いがもう起きないことであり、しかし足す指示の方は依然として読まれ突き合わされねばならない。4 つの経路のうち、外因性であり*かつ*足場を単に遊休させるのでなく能動的に有害にするのはこれだけである: 他の 3 つが残すのはコンテキストを食う足場だが、これが残すのは判断を食う足場である。時計はモデルのリリースサイクルであり、プラットフォームの機能サイクルより速い。そしてローカルの telemetry から最も見えにくい経路でもある — 全セッションで発火する rule は、その発火こそが問題になった時点まで、使用率 100% を報告し続ける。

### 補助輪のアナロジー

スキルは自転車の補助輪に相当する:
- 最初は補助輪がないと倒れる（スキルがないとサイクルが回らない）
- 乗れるようになったら補助輪は邪魔になる（スキルがコンテキストを消費するだけ）
- 補助輪を外しても自転車の乗り方は忘れない（サイクルの原則はルールとメモリに残る）

このアナロジーは最初の 3 つのメカニズムには当てはまり、第四には破れる。自転車に付けたままの補助輪は死重であって、舵を切らない。失効した制約は舵を切る — 乗り手自身のバランスに逆らって主張する。2026-07-25 の観測は、より重いフレームには正しかった角度で固定されたスタビライザーとして読むほうが正確である。

## What Survives: 情報差分の法則

溶解は単一の基準で artifact を選別する — 内容の高度さではなく、artifact とモデル/プラットフォームの既知との間の**情報差分**である:

- **差分が負 — 放置による失効を待たず、能動的に除去せねばならない。** 前提が失効した制約: 弱いモデルの最悪ケースに対するガードレールが、モデルが既に持つ能力に対して突き合わせるべき指示として読まれる状態。これらは剪定を待って遊休しているのではなく、生きた既定と矛盾し、その解決に推論を消費する。2026-07-25 の観測はこのクラスである
- **差分ゼロ — 溶解する。** 普遍的な手順 (コードレビューのチェックリスト、TDD の手順、build-fix のループ) は訓練データであり、プラットフォームが真っ先に吸収する
- **差分 100% — 残る。** 個人の文脈 (自分の repo 群、voice 規約、公開パイプライン)、生きた外部サービスの操作 (DOI レジストリ、dataset mirror、knowledge graph エンドポイント)、そしてハーネス自身の自己管理には、訓練データの代替が存在しない

負の極があるために、溶解を自然減に委ねることはできない。差分ゼロの artifact は単にコンテキストを浪費するだけで次の棚卸しまで待てるが、差分が負のものは、それが守るために書かれた振る舞いそのものを劣化させる。しかも残るほど機能している rule のように見える — 発火し続けるからである。

したがって汎用ハーネスは時間とともに、所有者の固有の実践のための operating system へと純化していく。「ニッチで異様」に見えるのは劣化ではない。汎用能力という水位の上昇が汎用的なものをすべて水没させたあとに残る姿である。

## 完了判定と溶解の順序

溶解は語りだけでなく観測の対象である。Measure フェーズが終了判定を供給する — 二段構成である (ADR-0022):

1. **Ablation (必要条件)。** ルールをロードした状態としていない状態で遵守計測 (skill-comply) を回す。場所はそのルールが運用されてきた文脈。両者が異なるなら、ルールはまだ荷重を受けている。ただし区別不能というだけでは証明書にならない — ルールのホーム分布上では、*内面化された* と *周囲の文脈が挙動を運んでいる* (あるいはそもそもそのタスク群でルールが荷重を受けていなかった) を分離できない。
2. **Held-out transfer (完了証拠)。** 同じ判断が held-out な文脈 — 原則は共有するが表面インスタンスを共有しない別 repo・別タスク種別・別セッション系譜 — で再現しなければならない。判断がローカルな文脈でなく永続層に住んでいることを証明するのは転移だけである。そこで初めてルールを削除してよい。

第二段を要求する外部証拠は EvoAgentBench (arXiv:2607.05202)。その ability-supported yet instance-disjoint な split は、skill の価値がホーム分布上ではなく転移で決まることを示した: curated な skill content は全 24 評価セルで正の転移、一方 automatic extraction はどの手法も全設定で正の利得を維持できず、software engineering では −36.3 点の負転移に達した。抽出された挙動の store は、作られた場所では有益に見えて、統制された一歩先では正味有害でありうる — ホーム文脈を出ない完了テストにはその差が見えない。

したがって退役判断は自らの証拠クラスを名乗る: **silence** (telemetry が不使用を示す — artifact を退役させるが内面化については何も主張しない。上の 2026-07-09 の退役はこのクラス)、**ablation** (必要条件)、**transfer** (完了証明書)。規範文書が自らの死亡判定基準を内蔵している。ADR-0022 がそれを一段から二段に研いだ。

溶解の予測順序は、差分の法則を AKC 自身に適用すれば導ける:

1. **最初**: 一般的に記述された行動原則 (Research、Curate) — プラットフォームが吸収するものに最も近い
2. **最後**: Extract と Promote の内側にある判断閾値 — *何を残す価値があるか* は運用者の taste をエンコードしており、普遍解が存在しない

最後に残る一行は、ルールではなく好みである。

## Implications

1. **AKC の成功指標はスキルの使用率ではない** — 使われなくなることが成功
2. **スキルは永続的なツールではなく、一時的な教材** — 概念を伝える役割を終えたら退役してよい
3. **サイクルの持続性はスキルではなくルールとメモリに依存する** — スキルを削除してもサイクルは回り続ける
4. **新しいユーザー/AI ペアにはスキルが再び必要** — 足場は人に依存する。別の文脈では再構築が必要
5. **プラットフォーム吸収は全員の足場を一斉に退役させる** — 人に依存する内面化と違い、ホストプラットフォームがネイティブに搭載した能力は、以後のすべてのユーザー/AI ペアにとって足場を不要にする
6. **モデル世代の交代は退役トリガーであり、その監査は後回しにできない** — substrate の判断力が上がると、前世代向けに書かれた制約は遊休から有害へと転じる。telemetry が満点であることは防御にならない: 常時ロードの rule は、その発火こそがコストになっているまさにその間、使用率 100% を報告する。したがってこの監査を予定するのは usage log ではなくモデルのリリースサイクルであり、ベンダー自身の prompt engineering ガイダンスがその証拠として採用できる

## Relationship to Design Principles

この概念は AKC の既存の設計原則と整合する:

- **Composable** — 各スキルが独立しているから、個別に不要になれる
- **Non-destructive** — スキルを削除しても蓄積されたルール・メモリ・ADR は残る
- **Observable** — skill-comply で「スキルなしでもサイクルが回っているか」を検証できる
